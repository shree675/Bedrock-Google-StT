mutation DeleteTranscripts($userid: String!) {
  deleteTranscripts(userid: $userid) {
    id
    title
    filetype
    expirationdate
    renderdate
    status
    transcript
    userid
    subtitle
    titlecolor
    audiourl
    imageurl
    subtitlecolor
    timestamps
  }
}
